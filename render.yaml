services:
  - type: web
    name: video-script-assistant-api
    env: node
    plan: free
    buildCommand: npm install && npm run build:client && npm run build:server
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
